
    <div class="row no-gutter">
        <!-- The image half -->
<div class="col-md-6 d-none d-md-flex">
      <%= image_tag "offer.png", width: 900  %>
    </div>        <!-- The content half -->
        <div class="col-md-6">
            <div class="login d-flex align-items-center py-5 pr-5">
                <!-- Demo content-->
                    <div class="row">
                      <div class="col-12 mx-auto">
                      <h3 class="mb-5">Inscrivez-vous pour nous rejoindre 🚀</h3>
                        <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
                            <%= f.error_notification %>

                            <div class="form-inputs">
                              <%= f.input :email,
                                          required: true,
                                          autofocus: true,
                                          input_html: { autocomplete: "email" },
                                          input_html: { class: "offer-bar" }%>
                              <%= f.input :first_name, label: "Prénom",
                                          input_html: { class: "offer-bar" }%>
                              <%= f.input :last_name, label: "Nom",
                                          input_html: { class: "offer-bar" }%>
                              <%= f.input :password,
                                          required: true,
                                          hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                                          input_html: { autocomplete: "Mot de passe" },
                                          label: "Mot de passe",
                                          input_html: { class: "offer-bar" }%>
                              <%= f.input :password_confirmation,
                                          required: true,
                                          input_html: { autocomplete: "Confirmation mot de passe" },
                                          label: "Confirmation mot de passe",
                                          input_html: { class: "offer-bar" }%>
                            </div>

                            <div class="form-actions">
                              <%= f.button :submit, "Je m'inscris", class: "btn btn-green mt-4" %>
                              <br/>
                              <br>
                              <a href="/users/sign_in" class="btn btn-dark">Déjà inscrit?</a>
                            </div>
                          <% end %>
                      </div>

                    </div>
            </div>
        </div><!-- End -->
    </div>



