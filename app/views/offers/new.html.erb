<div class="offset-3 col-8">
  <br/>
  <h3>Create a new offer</h3>


  <div class="form-group">
    <%= simple_form_for(@offer) do |f| %>
                  <%= f.input :title, label: "Titre offre" %>
                  <%= f.input :description, label: "Description" %>
                  <%= f.input :area, label: "Zone de déplacement" %>
                  <%= f.input :address, label: "Adresse" %>
                  <%= f.input :price_per_hour, label: "Prix par heure" %>
                  <%= f.input :photo, as: :file %>
                  <%= f.submit "Create offer", class: "btn btn-dark" %>

                <% end %>
  </div>
  <%= link_to  "Back to the list", offers_path %>
</div>

