<h1>coucou</h1>

<table class="table">
   <tr>
    <th>Title</th>
    <th>Description</th>
    <th>Voir</th>
    <th> Lien </th>
  </tr>
    <% @offers.each do |offer| %>
  <tr>
    <td><%= offer.title %></td>
    <td><%= offer.description  %></td>
    <td> <%= link_to  "Voir l'offre du buddy", offer_path(offer) %> </td>
  </tr>
  <% end %>
</table>
